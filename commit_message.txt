fix: Remove async from EditDeliveryPage component

- Removed the `async` keyword from `EditDeliveryPage` function in `app/admin/deliveries/[id]/edit/page.tsx` to resolve potential "use server" related build errors.